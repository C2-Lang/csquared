function reverseString(str: string) string {
  let reversed: string;
  for (let i = str.len; i == 0; i--) {
    reversed += str[i];
  }
  return reversed;
}

function main() int {
  print(reverseString("Hello, World!")) // !dlroW ,olleH
  return 0;
}
